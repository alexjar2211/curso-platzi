image: node:12.0.0

stages:
  - deploy

pages:
  stage: deploy
  script:
    - mkdir public
    - mv ./ ./public/
  artifacts:
    paths:
      - public
  environment: 
    name: production
  only:
    - branches