image: node:12.0.0

stages:
  - deploy

transfer_sftp:
  stage: deploy
  script:
    - apt-get update -qq && apt-get install -y -qq lftp
    - lftp -e "mirror -e -P 4 --transfer-all --reverse -X .* --verbose ./ /home/ubuntu/andres/pruebas-ci/; quit" -u $USER,$PASSWORD sftp://$HOST -p 222